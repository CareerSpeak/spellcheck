services:
  grammar:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: grammar
    expose:
      - 65535:65535
    networks:
      - careerspeak

networks:
  careerspeak:
    external: true
